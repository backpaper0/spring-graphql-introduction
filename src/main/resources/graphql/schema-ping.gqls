extend type Query {
	ping(notNull: Boolean = true, size: Int = 3): Ping!
}

type Ping {
	"任意の型T"
	ping1: String
	"List<T>"
	ping2: [String!]!
	"Optional<T>"
	ping3: String
	"Stream<T>"
	ping4: [String!]!
	"Mono<T>"
	ping5: String
	"Flux<T>"
	ping6: [String!]!
	"CompletionStage<T>"
	ping7: String
	"DataFetcherResult<T>"
	ping8: String
}
